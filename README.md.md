# My Sports Network

Single-file mobile web prototype. Tailwind CSS CDN + vanilla JS, no build step.

## Run locally
Just open `index.html` in a browser, or serve it:
```
npx serve .
```

## Deploy
Push to GitHub, then import the repo at vercel.com/new — Vercel auto-detects
a static site and deploys on every push to main.
